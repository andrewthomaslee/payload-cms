apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - ocirepository.yaml
  - helmrelease.yaml
  # TODO: generate sealed-secret.yaml with kubeseal (PEM pending deployment),
  # then add it here:
  # - sealed-secret.yaml
