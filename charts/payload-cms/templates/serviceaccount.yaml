{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "payload-cms.serviceAccountName" . }}
  labels:
    {{- include "payload-cms.labels" . | nindent 4 }}
{{- end }}
